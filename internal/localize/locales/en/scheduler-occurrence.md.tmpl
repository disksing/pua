PUA scheduled occurrence {{.OccurrenceID}}.

- Schedule: {{.ScheduleID}} revision {{.Revision}}
- Scheduled for: {{.ScheduledFor}}
- Coalesced through: {{.CoalescedThrough}} ({{.CoalescedCount}} nominal occurrence(s))
- Next occurrence: {{if .HasNext}}{{.NextRunAt}}{{else}}none{{end}}
- Original condition: {{.Condition}}
- Action: {{.Action}}
{{if .HasGuard}}- Guard: {{.Guard}}

Check the guard now. If it is false, end this Turn without performing the action. When the target is a Task, first move it out of `in_progress` to the appropriate state so its workflow does not create an automatic continuation.{{else}}- Guard: none
{{end}}
This mailbox message is at-least-once. Before causing external side effects, use the occurrence ID to make the action idempotent.
